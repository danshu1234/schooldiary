import { FC } from "react";
import InitialSchedule from "./(InitialSchedule)/initialSchedule";
const Registration: FC = () => {
    return (
        <div>
        <InitialSchedule/>
        </div>
    )
}

export default Registration